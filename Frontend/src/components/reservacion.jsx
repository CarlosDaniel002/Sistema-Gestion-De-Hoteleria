import React from "react";
import Navigation from "../components/Navigation";
const Reservation = () => {
  return(
  <div>
    <Navigation/>
    <p>Reservacion</p>
  </div>);
}

export default Reservation